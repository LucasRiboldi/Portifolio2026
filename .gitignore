# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# brainstorm artifacts
.superpowers/

.vercel

*storybook.log
storybook-static

# scratch/temp scripts (nunca commitar — podem conter segredos)
_*.mjs
.env.vercel.tmp
.playwright-mcp/

# originais pesados do deck ThunderCats — só as webs otimizadas (/web) entram no git
/public/cards-thundercats/*
!/public/cards-thundercats/web/

# clones de referência para estudo dos efeitos holo (28 MB).
# Ficam fora do git de propósito: tudo em /public é servido publicamente e
# entra no deploy — não faz sentido publicar o código-fonte de terceiros.
# Para reler: git clone dos repos simeydotme/hover-tilt e /pokemon-cards-css
/public/hover-tilt-main/
/public/pokemon-cards-main/
