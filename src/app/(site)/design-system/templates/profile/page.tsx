import { ProfileTemplateContent } from "./content"

/** Rota fina: o conteudo vive em content.tsx e e reusado pelo documento unico. */
export default function ProfileTemplatePage() {
  return <ProfileTemplateContent />
}
