/**
 * Stub de `server-only` para os testes unitários.
 *
 * O pacote real é um shim do Next que quebra o build de propósito se o módulo
 * for importado no client. Fora do Next ele nem resolve — e a marcação não tem
 * relevância para testar funções puras.
 */
export {}
